package ${config.basePackage}.service;

import com.baomidou.mybatisplus.extension.service.IService;
import ${config.basePackage}.po.${model.model};

/**
* 表[${model.table}]对应的服务类
*
* @author ${config.author}
*/

public interface ${model.model}Service extends IService<${model.model}> {
}
